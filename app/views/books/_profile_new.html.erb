<br>
<br>
<br>
  <% if book.errors.any? %>
    <p><%= book.errors.count %> error prohibited this book form being saved:</p>
    <ul>
    <% book.errors.full_messages.each do |message| %>
      <li><%= message %></li>

    <% end %>
    </ul>
  <% end %>

  <h2>User info</h2>
  <%= attachment_image_tag user, :profile_image, fallback: "no_image.jpg", size: "100x100" %>

  <table class="table">
    <tbody>
      <tr></tr>
      <tr>
        <th>name</th>
        <th><%= user.name %></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th><%= user.introduction %></th>
      </tr>
    </tbody>
  </table>

  <div class="row">

    <%= link_to edit_user_path(user)  do %>
      <button type ="button" class= "col-xs-12 btn btn-default"><span class="col-xs-12 glyphicon glyphicon-wrench" ></span>
      </button>
    <% end %>
  </div>


<h2>New book</h2>


<!-- form_for -->
<%= form_for(book_new) do |f| %>

<div class="field row">
    <label for="book_title">Title</label>
    <input class="col-xs-12 book_title" type="text" name="book[title]" id="book_title">
  </div>

<div class="field row">

    <label for="book_opinion">Opinion</label>
    <textarea class="col-xs-12 book_body" name="book[body]" id="book_body"></textarea>
</div>
<br>
<div class="actions row">
  <button type="submit" class="bbtn btn-primary col-xs-12">Create Book</button>
  </div>

<% end %>
  </ul>
