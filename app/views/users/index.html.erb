<%= render 'books/profile.new', book:@book %>
<h2>Users</h2>
<% @users.each do |user| %>
<%= attachment_image_tag user, :profile_image, fallback: "no_image.jpg", size: "100x100" %>
<p><%= user.name %></p>
<%= link_to "Show", users_path(user) %>
<% end %>
